package com.sf.util;

import com.sf.model.Log;

/**
 * 
 * 上午7:04:50
 * @author sf
 * TODO
 */
public class LogUtil {

    public Log setLog(String userid, String time, String detail, String ip){
         Log log = new Log();
        log.setUserid(userid);
        log.setTime(time);
        log.setDetail(detail);
        log.setIp(ip);
        return log;
    }

}
